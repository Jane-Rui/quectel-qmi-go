name: Sync upstream (cm9vdA/quectel-qmi-go)

on:
  schedule:
    - cron: '11 3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout fork
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Add upstream remote
        run: git remote add upstream https://github.com/cm9vdA/quectel-qmi-go.git 2>/dev/null || true

      - name: Fetch and merge upstream
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git fetch upstream master
          LOCAL=$(git rev-parse HEAD)
          UPSTREAM=$(git rev-parse upstream/master)

          if [ "$LOCAL" = "$UPSTREAM" ]; then
            echo "已是最新，无需同步"
            exit 0
          fi

          BEHIND=$(git rev-list HEAD..upstream/master --count)
          echo "上游有 ${BEHIND} 个新提交，开始合并..."

          git merge upstream/master --no-edit -m "sync: 自动同步上游 cm9vdA/quectel-qmi-go (${BEHIND} 个新提交)"
          git push origin master

          echo "✅ 同步完成"
